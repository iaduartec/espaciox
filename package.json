{
  "name": "espaciox",
  "version": "1.0.0",
  "type": "module",
  "description": "Landing estática para el salón privado El Santuario en Burgos. Incluye páginas de marketing, sección de blog y formularios básicos para simulación de reservas.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "postcss assets/css/styles.css -o assets/css/styles.css && csso assets/css/styles.css --output assets/css/styles.min.css --comments exclamation",
    "build:js": "terser assets/js/main.js -o assets/js/main.min.js --compress --mangle",
    "build": "npm run build:css && npm run build:js",
    "validate:headers": "node scripts/header-validator.js",
    "check:meta": "node scripts/check-meta.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "autoprefixer": "^10.5.0",
    "csso-cli": "^4.0.2",
    "dotenv": "^17.4.2",
    "lighthouse": "^13.4.0",
    "postcss": "^8.5.15",
    "postcss-cli": "^11.0.1",
    "puppeteer": "^25.1.0",
    "sharp": "^0.35.2",
    "terser": "^5.48.0"
  },
  "browserslist": [
    ">0.5%",
    "last 2 versions",
    "not dead",
    "not ie <= 11"
  ]
}
