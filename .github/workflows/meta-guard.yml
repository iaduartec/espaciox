name: Meta Guard

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  seo-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: npm install
      - run: npm run check:meta
