# Clave para la extensión VSCode-OpenAI (no se sube una clave real)
OPENAI_API_KEY=
# Base URL opcional para la API (deja el valor por defecto si no necesitas personalizarlo)
OPENAI_API_BASE_URL=https://api.arbs.io/openai/inference/v1
